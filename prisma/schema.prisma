// This is your Prisma schema file.
// Run `npx prisma db push` to sync with your database.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── App Models ───────────────────────────────────────────────

model Craving {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  intensity Int      // 1–10 scale
  trigger   String?  // e.g. "stress", "after coffee", "boredom"
  notes     String?  // free text
  resisted  Boolean  @default(false)
  location  String?  // e.g. "home", "office", "outside"

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
  @@index([createdAt])
}

model TriviaQuestion {
  id         String   @id @default(cuid())
  question   String   @unique
  answers    String[]
  correct    Int
  category   String   // "geography" | "science" | "math" | "history" | "wordplay" | "riddle" | "culture" | "language"
  type       String   // "trivia" | "math" | "riddle" | "wordplay" | "puzzle"
  difficulty Int      @default(1) // 1=easy, 2=medium, 3=hard

  createdAt  DateTime @default(now())

  @@index([category])
  @@index([type])
}

// ─── NextAuth Models ──────────────────────────────────────────

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())

  accounts          Account[]
  sessions          Session[]
  cravings          Craving[]
  userOptions       UserOption[]
  pushSubscriptions PushSubscription[]
}

model UserOption {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  type      String   // "trigger" | "location"
  value     String

  createdAt DateTime @default(now())

  @@unique([userId, type, value])
  @@index([userId, type])
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model PushSubscription {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  endpoint  String   @unique
  p256dh    String
  auth      String

  createdAt DateTime @default(now())

  @@index([userId])
}
